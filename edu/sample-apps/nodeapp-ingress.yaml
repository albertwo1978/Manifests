apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
  name: nodeapp-ingress
spec:
  tls:
  - hosts:
    - nodeapp.13.68.88.114.xip.io
    secretName: xipio-tls-cert
  rules:
    - host: nodeapp.13.68.88.114.xip.io
      http:
        paths:
          - path: /
            backend:
              serviceName: frontend
              servicePort: 8080  